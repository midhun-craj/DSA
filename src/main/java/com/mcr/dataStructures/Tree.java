package com.mcr.dataStructures;

public class Tree {

    /// A tree is a non-linear data structure where nodes are organized in a hierarchy.
    /// ex: File explorer, Databases, DNS, HTML DOM
    ///
    /// A subtree is a smaller tree held within a larger tree.
    /// Size of a tree is the no of nodes present in the tree.
    ///
    /// Depth of a node is the number of edges from the root node to that node.(root node has a depth of 0)
    /// Height of a node is the number of edges on the longest path from that node down to a leaf node.(leaf node
    /// has a height of 0)

}
